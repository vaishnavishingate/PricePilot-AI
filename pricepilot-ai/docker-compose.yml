version: '3.8'

services:
  pricepilot-app:
    build: .
    container_name: pricepilot_ai_container
    ports:
      - "8000:8000"
    volumes:
      - ./pricepilot.db:/app/pricepilot.db
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
